<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tutorfinder">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TutorFinder"
        tools:replace="android:icon">

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.StudentActivity" />

        <activity android:name=".activities.TutorActivity" />

        <activity
            android:name=".activities.LoginActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:noHistory="true" />

        <activity android:name=".activities.SignUpActivity" />

        <activity android:name=".activities.SelectRoleActivity" />

        <activity android:name=".activities.StudentRegistrationActivity" />

        <activity android:name=".activities.TutorRegistration" />
        <activity android:name=".activities.TutorRegistrationActivity" />

        <activity android:name=".activities.TutorDetailsActivity" />

    </application>

</manifest>